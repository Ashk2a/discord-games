export enum GameEvent {
    // Game events
    CREATED = 'game-created', // (game: Game)
    // Member events
    MEMBER_JOIN = 'member-joined', // (member: Member)
    MEMBER_LEFT = 'member-left', // (member: Member)
    // Player events

    // Dealer events
}